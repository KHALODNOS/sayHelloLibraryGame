from setuptools import setup, find_packages

setup(
    name='welcomelib',
    version='0.1',
    packages=find_packages(),
    description='A simple welcome message library',
    author='Y.K.M',
    license='MIT',
)
